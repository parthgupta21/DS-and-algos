// DSA Standard Library Template
#include <iostream>      // For input/output
#include <vector>        // For dynamic arrays
#include <array>         // For fixed-size arrays
#include <deque>         // For double-ended queue
#include <list>          // For doubly linked list
#include <stack>         // For stack data structure
#include <queue>         // For queue data structures
#include <set>           // For ordered set
#include <unordered_set> // For unordered set (hash set)
#include <map>           // For ordered map (RB-tree based)
#include <unordered_map> // For unordered map (hash map)
#include <algorithm>     // For common algorithms (sort, reverse, etc.)
#include <numeric>       // For numeric operations (accumulate, gcd, etc.)
#include <utility>       // For pair and utility functions
#include <cmath>         // For mathematical functions
#include <climits>       // For INT_MIN, INT_MAX
#include <cstring>       // For memset, string operations
#include <string>        // For string manipulation
#include <bitset>        // For bitwise operations and bitset container
#include <tuple>         // For tuple container
#include <functional>    // For function objects like greater<int>
#include <iterator>      // For iterator functions
#include <cassert>       // For debugging with assert
#include <iomanip>       // For manipulating output precision
#include <sstream>       // For string streams

// Common aliases to simplify code:
using namespace std;

// Aliases for common data structures:
typedef vector<int> vi;                                        // Vector of integers
typedef vector<vector<int>> vvi;                               // Vector of vector of integers
typedef pair<int, int> pii;                                    // Pair of integers
typedef long long ll;                                          // Long long integer
typedef vector<ll> vll;                                        // Vector of long long integers
typedef priority_queue<int> pq;                                // Max-heap priority queue
typedef priority_queue<int, vector<int>, greater<int>> min_pq; // Min-heap priority queue

// Utility macros for loops and debugging
#define rep(i, a, b) for (int i = a; i < b; ++i)
#define all(x) (x).begin(), (x).end()              // Shorthand for the entire range
#define rall(x) (x).rbegin(), (x).rend()           // Shorthand for reverse range
#define sz(x) ((int)(x).size())                    // Get size of container
#define F first                                    // First element of a pair
#define S second                                   // Second element of a pair
#define pb push_back                               // Push back in vector
#define mp make_pair                               // Make pair
#define debug(x) cout << #x << " = " << x << endl; // Debugging macro

int main()
{
    

    return 0;
}
