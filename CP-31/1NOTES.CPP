//An arrangement that always minimizes the absolute difference between adjacent pairs is the array in sorted order.

// maximum can sometimes means finding gcd if you are n it given an >= sign in the condition  // can look at the 9th question of 900 rated CP-31




// For a collection of integers S, define mex(S) as the smallest non-negative integer that does not appear in S.